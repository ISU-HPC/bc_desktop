---
batch_connect:
  template: vnc

script:
  accounting_id: "<%= slurm_account.delete(' ') %>"
  native:
    - "--ntasks=<%= num_cores.blank? ? 1 : num_cores.to_i %>"
    - "--nodes=<%= bc_num_slots.blank? ? 1 : bc_num_slots.to_i %>"
    - "--mem=<%= mem_req %>" 
    - "--job-name=<%= job_name %>"
    - "--partition=<%= custom_queue.delete(' ') %>"  
    <%- if custom_queue.include? "gpu" or custom_queue.include? "a100" -%>
    - "--gres=<%= gres %>"
    <%- end -%>
